SELECT c.CategoryName, COUNT(p.ProductID) AS TotalProdutosAcimaDaMedia
FROM Products p 
JOIN Categories c ON p.CategoryID = c.CategoryID
WHERE p.Price > (
SELECT AVG(p2.Price) 
FROM Products p2 
WHERE p2.CategoryID = p.CategoryID
)
GROUP BY c.CategoryName;
