SELECT 
    ProductID,
    SUM(Quantity) AS Total_Vendido
FROM OrderDetails
WHERE ProductID IN (
    SELECT ProductID
    FROM Products
    WHERE Price > 20
)
GROUP BY ProductID
ORDER BY Total_Vendido DESC
LIMIT 5;
